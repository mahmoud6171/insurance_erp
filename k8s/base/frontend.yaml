apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: insurance-erp
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: insurance-erp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: insurance-erp-frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
