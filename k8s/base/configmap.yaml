apiVersion: v1
kind: ConfigMap
metadata:
  name: insurance-erp-config
  namespace: insurance-erp
data:
  DEBUG: "False"
  ALLOWED_HOSTS: "*"
  CORS_ALLOWED_ORIGINS: "http://localhost,http://localhost:80,http://frontend"
  EMAIL_HOST: "smtp.gmail.com"
  EMAIL_PORT: "587"
  DEFAULT_FROM_EMAIL: "noreply@insurance-erp.com"
